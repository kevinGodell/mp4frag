'use strict';

const tags = ['hev1', 'hvc1'];

const profiles = ['main', 'main10', 'main444-8', 'main444-10', 'main12', 'main422-12', 'main444-12'];

const levels = ['10', '20', '21', '30', '31', '40', '41', '50', '51', '52', '60', '61', '62', '85'];

const pixFmts = ['yuv420p', 'yuv422p', 'yuv444p', 'yuv420p10le', 'yuv444p10le', 'yuv420p12le', 'yuv422p12le', 'yuv444p12le'];

const keyints = [1, 2];

const noHighTiers = [0, 1];

module.exports = {
  tags,
  profiles,
  levels,
  pixFmts,
  keyints,
  noHighTiers,
};
